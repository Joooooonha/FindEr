# CLAUDE.md

이 프로젝트의 메인 가이드는 [`AGENTS.md`](./AGENTS.md)다.

Claude Code를 포함한 모든 AI 코딩 에이전트(Codex, Cursor, Aider 등)는 `AGENTS.md`를 공통 컨텍스트로 사용한다.
규칙·아키텍처·컨벤션·진행 상황은 모두 `AGENTS.md`에 기록한다.

이 파일은 Claude Code 전용 지침이 필요할 때만 별도로 갱신한다.
별도 지침이 없으면 비워두며, 기본은 `AGENTS.md`를 가리키는 포인터다.
